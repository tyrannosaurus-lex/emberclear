<div>
  <h4>Notifications</h4>

  {{#if this.isEnabled}}
    <button {{action this.disable}} class='button'>Disable</button>
  {{else}}
    <button {{action this.askNotifications}} class='button'>Enable</button>
  {{/if}}
</div>

